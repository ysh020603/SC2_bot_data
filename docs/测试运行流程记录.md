# SC2 Agent 测试运行流程记录

本文记录 2026-06-16 用 `DeepSeek-V4-flash` 跑通单局测试时的可复用流程。

## 已验证环境

- 工作目录：`C:\code\SC2_Agent_OLD`
- Conda 环境：`C:\Users\Descfly\.conda\envs\SC2_0615`
- Python：使用该环境内的 `python.exe`
- 记录目录统一放在：`C:\code\SC2_Agent_OLD\game_records`
- 本次成功样例：
  - 对手：内置 AI `Terran / hard / air`
  - 模型：`DeepSeek-V4-flash`
  - 结果：Victory
  - 目录：`game_records\v4_flash_test\20260616_110844_v4_flash_t_air_hard`

## 跑之前检查

```powershell
cd C:\code\SC2_Agent_OLD

& 'C:\Users\Descfly\.conda\envs\SC2_0615\python.exe' -m py_compile `
  run_vs_ai.py `
  bot_loader\game_starter.py `
  dummies\generic\universal_llm_bot.py `
  SC2_Agent\naming_agent.py
```

可选：确认没有残留对局进程。

```powershell
Get-Process python -ErrorAction SilentlyContinue |
  Select-Object Id,ProcessName,Path,StartTime
```

## 推荐启动方式

Windows 下直接用很长的 `run_vs_ai.py` 默认 match id 容易触发日志路径过长。推荐用一个短 match id 的临时 launcher。模板如下，保存到 `tools\run_<test_name>.py`，跑完删除。

```python
"""Temporary launcher for one short-path test match."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import run_vs_ai


def short_match_id(**kw):
    return f"{kw['timestamp']}_v4_flash_t_air_hard"


run_vs_ai.build_match_id = short_match_id

run_vs_ai.play_vs_ai(
    enemy_race="terran",
    enemy_difficulty="hard",
    enemy_build="air",
    top_model="DeepSeek-V4-flash",
    mid_model="DeepSeek-V4-flash",
    down_model="DeepSeek-V4-flash",
    naming_model="DeepSeek-V4-flash",
    ordering_model="DeepSeek-V4-flash",
    executor_model="DeepSeek-V4-flash",
    batch_name="v4_flash_test",
    output_base_dir=r"C:\code\SC2_Agent_OLD\game_records",
    skip_version_update=True,
)
```

启动命令：

```powershell
$env:PYTHONUTF8='1'
$env:PYTHONIOENCODING='utf-8'

$py='C:\Users\Descfly\.conda\envs\SC2_0615\python.exe'
$out=Join-Path (Get-Location) 'game_records\v4_flash_test_stdout.log'
$err=Join-Path (Get-Location) 'game_records\v4_flash_test_stderr.log'

$p=Start-Process -FilePath $py `
  -ArgumentList @('tools\run_v4_flash_test.py') `
  -WorkingDirectory (Get-Location) `
  -RedirectStandardOutput $out `
  -RedirectStandardError $err `
  -PassThru `
  -WindowStyle Hidden

"PID=$($p.Id)"
```

## 运行中观察

确认进入游戏、对手配置正确：

```powershell
Get-Content game_records\v4_flash_test_stderr.log -Tail 120
```

关键日志应包含：

```text
Players: Bot UniversalLLMBot(Terran), Computer Hard(Terran, Air)
[strategy_steps] loaded 7 step(s) from strategy 'marine_rush'.
MACRO PIPELINE START (trigger=initial_step
Strategy step 1 ...
```

检查 step 推进：

```powershell
Select-String -Path game_records\v4_flash_test_stderr.log `
  -Pattern 'Strategy step|Stage2 named|Installed|Result for player|Result:' |
  Select-Object -Last 80 |
  ForEach-Object { $_.Line }
```

期望流程：

- Step 1 到 Step 6 依次出现。
- Step 7 出现后会重复出现。
- `Stage2 dropped SupplyDepot (handled by Stage5).` 是正常的，表示 SupplyDepot 被过滤，补给由 Stage5 自动插入。

## 结束后确认

```powershell
Select-String -Path game_records\v4_flash_test_stderr.log `
  -Pattern 'Result for player|Result:|Victory|Defeat|Tie' |
  Select-Object -Last 20 |
  ForEach-Object { $_.Line }
```

查看产物：

```powershell
Get-ChildItem game_records\v4_flash_test\<match_id> |
  Select-Object Name,Length,LastWriteTime
```

完整产物应包含：

- `<match_id>.log`
- `<match_id>.json`
- `<match_id>.llm_calls.json`
- `<match_id>.SC2Replay`

## 常见坑

- `UnicodeEncodeError`：先设置 `$env:PYTHONUTF8='1'` 和 `$env:PYTHONIOENCODING='utf-8'`。
- 路径过长导致 `.log` 创建失败：使用短 `match_id`，并放在 `game_records\<batch_name>\<short_match_id>`。
- `conda` 不在 PATH：直接调用 `C:\Users\Descfly\.conda\envs\SC2_0615\python.exe`。
- `Start-Process -ArgumentList @('-c', '<多行代码>')` 容易被 Windows 参数拆坏：用临时 launcher 脚本更稳。
- `Action module SKILL.terran.Action not found; action space will be empty.` 本次测试中出现过，但不影响当前 step pipeline 跑完。
- 临时 launcher 跑完要删除，避免污染工具目录。

## 修改测试参数

常改项在 launcher 的 `play_vs_ai(...)` 中：

- `enemy_race`: `"terran" | "zerg" | "protoss" | "random"`
- `enemy_difficulty`: `"medium" | "hard" | "harder" | "veryhard"` 等
- `enemy_build`: `"air" | "macro" | "rush" | "timing" | "power"`
- `map_name`: 例如 `"KairosJunctionLE"`、`"AutomatonLE"`；必须是 `bot_loader/game_starter.py` 打印的已安装地图名
- `*_model`: 改成 `API_config/config.json` 里存在的 model key
- `batch_name`: 建议每组测试一个独立名字
- `short_match_id`: 保持短，包含核心变量即可

## battle_cruisers 建筑起飞回归测试

2026-06-16 针对 `SKILL\terran\battle_cruisers` 的建筑起飞问题，推荐使用如下短 launcher：

```python
"""Temporary launcher for the battle_cruisers addon/placement test."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import run_vs_ai


def short_match_id(**kw):
    return f"{kw['timestamp']}_bc_addonfix"


run_vs_ai.build_match_id = short_match_id

run_vs_ai.play_vs_ai(
    map_name="KairosJunctionLE",
    enemy_race="terran",
    enemy_difficulty="medium",
    enemy_build="air",
    top_model="DeepSeek-V4-flash",
    mid_model="DeepSeek-V4-flash",
    down_model="DeepSeek-V4-flash",
    naming_model="DeepSeek-V4-flash",
    ordering_model="DeepSeek-V4-flash",
    executor_model="DeepSeek-V4-flash",
    batch_name="battle_cruisers_test_addonfix",
    output_base_dir=r"C:\code\SC2_Agent_OLD\game_records",
    skip_version_update=True,
    force_strategy="battle_cruisers",
)
```

换地图回归时，只改 `map_name`，例如：

```python
map_name="AutomatonLE"
```

关键检查命令：

```powershell
Select-String -Path game_records\battle_cruisers_test_addonfix_stderr.log `
  -Pattern 'FACTORYFLYING|STARPORTFLYING|BARRACKSFLYING|Inserted gap-fill|LandFlyingProduction|BuildAddon|GridBuilding FUSIONCORE|Result:' |
  Select-Object -Last 120 |
  ForEach-Object { $_.Line }
```

预期：

- Factory Tech Lab 出现 `BuildAddon UnitTypeId.FACTORYTECHLAB ...`。
- Starport Tech Lab 出现 `BuildAddon UnitTypeId.STARPORTTECHLAB ...`。
- 不应出现 `FACTORYFLYING`、`STARPORTFLYING`、`BARRACKSFLYING`。
- 不应出现 `Inserted gap-fill`。
- 不应出现 `LandFlyingProduction` 反复找落点。

已验证：

- `KairosJunctionLE` / `Terran medium air`：FactoryTechLab、StarportTechLab、FusionCore 正常，未复现起飞。
- `AutomatonLE` / `Terran medium air`：FactoryTechLab、StarportTechLab 正常，未复现起飞。
